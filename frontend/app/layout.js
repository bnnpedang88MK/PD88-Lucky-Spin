import './globals.css';

export const metadata = {
  title: 'PEDANG88 Lucky Spin Analytics',
  description: 'Analytics dashboard for Lucky Spin operations'
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
